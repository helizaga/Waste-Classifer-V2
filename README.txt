I used a pre-trained residual convolutional neural network (Inception V3) and, through transfer learning, trained the last layer with a dataset of 2,500 images of trash, obtaining an accuracy of 91% in the test set.

Link to test project: https://react-flask-waste-classifier.herokuapp.com/
